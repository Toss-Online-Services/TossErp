import 'package:flutter/material.dart';
// Legacy dashboard layout screen disabled pending reimplementation with SimpleDashboardManager.

/// Screen for selecting and managing dashboard layouts
class DashboardLayoutScreen extends StatelessWidget {
  const DashboardLayoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Dashboard layout customization coming soon')),
    );
  }
}
