# POS Test Runner Scripts

# Run all POS E2E tests
pnpm run test:e2e:pos

# Run POS tests in UI mode (interactive)
pnpm run test:e2e:pos:ui

# Run POS tests with debug
pnpm run test:e2e:pos:debug

# Generate HTML report
pnpm run test:e2e:pos:report
