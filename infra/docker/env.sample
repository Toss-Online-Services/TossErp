# Local development defaults

POSTGRES_USER=toss
POSTGRES_PASSWORD=toss
POSTGRES_DB=toss

# JWT and secrets (set real values for production)
JWT_AUTHORITY=http://localhost:8080
JWT_AUDIENCE=toss-api

# Gateway downstreams (overridable per environment)
INVENTORY_BASE_URL=http://inventory:8080
SALES_BASE_URL=http://sales:8080
COLLAB_BASE_URL=http://collaboration:8080



