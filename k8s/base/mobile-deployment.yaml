apiVersion: apps/v1
kind: Deployment
metadata:
  name: mobile
  namespace: toss-erp
  labels:
    app.kubernetes.io/name: mobile
    app.kubernetes.io/instance: toss-erp
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: mobile
    app.kubernetes.io/part-of: toss-erp
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: mobile
      app.kubernetes.io/instance: toss-erp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mobile
        app.kubernetes.io/instance: toss-erp
        app.kubernetes.io/version: "1.0.0"
        app.kubernetes.io/component: mobile
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
      - name: mobile
        image: ghcr.io/toss-online-services/toss-erp/mobile:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: API_BASE_URL
          value: "http://gateway"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 1001
          capabilities:
            drop:
            - ALL
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: mobile
  namespace: toss-erp
  labels:
    app.kubernetes.io/name: mobile
    app.kubernetes.io/instance: toss-erp
    app.kubernetes.io/component: mobile
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: mobile
    app.kubernetes.io/instance: toss-erp
